# DeepSeek reference inference snapshots

- `V3/`: copied `inference/` from `deepseek-ai/DeepSeek-V3` (includes `generate.py`).
- `V3.2/`: copied `inference/` from `deepseek-ai/DeepSeek-V3.2-Exp`.
- `V4/`: copied `inference/` from Hugging Face [`deepseek-ai/DeepSeek-V4-Pro`](https://huggingface.co/deepseek-ai/DeepSeek-V4-Pro).

Model checkpoints are **not** included. See each `SOURCE_REVISION.txt` for the exact Git commit.
